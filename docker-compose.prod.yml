services:
  web:
    image: ${WEB_IMAGE:-ghcr.io/henrrycoronado/u-voluntapp-web:latest}
    restart: unless-stopped
    ports:
      - "5173:80"
    environment:
      - VITE_API_BASE_URL=${VITE_API_BASE_URL}
