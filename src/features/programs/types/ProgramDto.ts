export interface ProgramDto {
  uvaCode: string;
  name: string;
  description?: string;
  acronym?: string;
  color?: string;
  profilePhotoUrl?: string;
  coverPhotoUrl?: string;
  stateCode: string;
  createdAt: string;
}
